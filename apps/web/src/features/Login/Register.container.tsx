import RegisterForm from '~/features/Login/ui/RegisterForm.tsx';
import SingWrapper from '~/features/Login/ui/SingWrapper.tsx';

const RegisterContainer = () => {
  return (
    <SingWrapper>
      <RegisterForm />
    </SingWrapper>
  );
};

export default RegisterContainer;
